import { HighlightSearchTextPipe } from './highlight-search-text.pipe';

describe('HighlightSearchTextPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightSearchTextPipe();
    expect(pipe).toBeTruthy();
  });
});
